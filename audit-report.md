# Aoide Audit Report

deep · hybrid · fix=false (no fixes applied — findings only)
Generated by a five-persona review fleet: Type Sentinel · Logic Architect · Performance Oracle · Ecosystem Integrator · Quality Guardian.

**TOTAL: 0 critical · 4 high · 13 medium · 9 low · 12 note**

---

## HIGH

- `pkgs/aoide/crates/song/src/commands/draft.rs:399,412` — `aoide rice draft drop <name>` builds `draft_dir(song, name)` from an unvalidated CLI name and `remove_dir_all`s it — `draft drop ../../x` (an agent-safe, non-gated command) deletes an arbitrary directory. `valid_song_name` exists; it's just not applied here.
- `pkgs/aoide/crates/song/src/live.rs:38-80` — `to_hyprland_rgb` interpolates the raw `window.border` livery string into a `;`-joined `hyprctl --batch`; a border of `0; dispatch exec <cmd>` becomes a second command. `schema::is_hex` exists but the hot path (`handle_rice_stage`) never lints first.
- `pkgs/aoide/crates/conduct/src/graph/session_store.rs:241-264` — `set_owner_activity` rewrites hooks.json + sessions.json + graph.json on *every* tool hook with no change-diff, while the sibling `do_session_refresh` is change-gated.
- `modules/facets/quickshell/qml/ConductorGadget.qml:313-414` — `rebuild()` reassigns `temple.groups` fresh on every sessions/hooks/projects reload → all SessionCard delegates (nested Repeaters, infinite animations, kaomoji frames) are torn down and recreated each heartbeat. TerminalsGadget already has the `_rosterSig` gate; the Conductor lacks one.

## MEDIUM

- `modules/facets/quickshell/qml/TerminalsGadget.qml:221` — `terminalRows()` drops `needsSudo` from the row object, so the delegate's sudo badge and urgent border can never fire.
- `pkgs/aoide/crates/conduct/src/graph/window.rs:398` — `ensure_session_window` does an unlocked load-mutate-write of sessions.json, racing locked writers (same clobber class send.rs already fixed elsewhere).
- `pkgs/aoide/crates/conduct/src/graph/window.rs:527` — `resolve_pending_session_windows` / `clear_closed_window` (listener thread) write sessions.json without `with_stage_lock`.
- `pkgs/aoide/crates/conduct/src/graph/send.rs:135` — `record_pending` / `set_session_title` do unlocked read-modify-write of pending.json / sessions.json.
- `pkgs/aoide/crates/song/src/commands/rice.rs:237,262,282` — `rice stage <name>` uses the positional name unvalidated in read/write paths and poisons `current_staged_song` for later `draft drop`/`mode` commands.
- `pkgs/aoide/crates/song/src/commands/draft.rs:199-223` + `mode.rs:136,346` — `current_staged_song()` returns the stage file's `"song"` string unvalidated; a hand-edited stage file escapes `songbook/<song>/drafts/`.
- `pkgs/aoide/crates/storage/src/peer_store.rs:191` + `client/src/commands.rs:295,384-399` — unvalidated peer nickname joined into `state/peer-cache/<name>.json` paths.
- `song/songbook/sonata/widgets/bar.qml:1360` — the ✎N sessions-cell click sends `{cmd:"dock",action:"toggle"}` but shellbridge parses no dock command → silent no-op.
- `lib/checks.nix:111` — house rule 5 claims the checks fail eval on livery-only violations, but no such coupling check is exported; the claim outruns the enforcement.
- `song/songbook/sonata/widgets/bar.qml:1893-1904` — stale "this rig has no input synthesis" comment, contradicted by hazards.md §6 in the same dirty set.
- `bar.qml:1503-1504,1533-1534,1941-1944` + `AudioColonnade.qml:560-561,1089-1090` — hazards.md documents the horizontal-wheel bug and prescribes `angleDelta.y !== 0` gates; all six `onWheel` handlers still ship the unfixed `> 0 ? 2 : -2` form.
- `bar.qml:1834-1839` vs `AudioColonnade.qml:419-429` — two hand-rolled `kaomojiFor()` implementations disagree on the same audio state (mic-only muted).
- `tools/pointer.sh:157-176` — bare `click`/`scroll` act at the current position with no coordinate or drift guard; the pre-flight guard is opt-in only.

## LOW

- `pkgs/aoide/crates/conduct/src/graph/window.rs:827` — untrusted hyprctl pid narrowed `as i32` then re-widened `as u32`; a >i32::MAX value silently wraps before `/proc/<pid>/cwd`.
- `modules/facets/quickshell/qml/TerminalsGadget.qml:248` — `rosterSig` joins fields with `""` and no delimiter — not injective, a real change can be masked.
- `pkgs/aoide/crates/conduct/src/reap.rs:418` — the reaper holds the stage lock across the `hyprctl clients -j` subprocess; a hung hyprctl blocks all stage writers.
- `pkgs/aoide/crates/conduct/src/graph/conduct.rs:579-651` — O(conns²) revents lookup per poll iteration.
- `AoidePanel.qml` — dock is always `visible:true`, sliding off via Translate, so every gadget timer runs 24/7 while hidden.
- `TerminalsGadget.qml:74,256-329` — 1s `nowMs` tick re-renders elapsed on every row; `rosterSig` includes volatile fields (activity/say).
- `tools/pointer.sh:245` — needs ImageMagick but nothing installs it on PATH (stylix is build-time only).
- `tools/pointer.sh:118-122` — idle gate is advisory, never called by the act commands; a human typing with a still mouse passes it.
- `hosts/_desktop/default.nix:49` — `aoide.pointer.enable` is only set in the shelved `_desktop` template, not on the live `yomi-strix` host, so pointer.sh always takes the `nix build` fallback.

## NOTE

- `client/src/commands.rs:38` — SSRF: a2a/peer URLs unguarded (agent-reachable localhost).
- `conduct/src/graph/send.rs` — `graph send` connects to the socket field read from the (untrusted) stage file.
- `shell.qml:55` — the `-1` no-workspace sentinel collides with any special workspace whose id is exactly -1 (asserted, not verified).
- `TerminalsGadget.qml:287` + `ConductorGadget:1518-1527` — shared hover state written by two widgets with different clear timing.
- `doc.rs:469-477` — `restage_graph` reads peers from disk on every stage mutation.
- `options.nix:6` — "facets read only aoide.livery" is stricter than the code (facets also read nucleus contract options).
- `pointer.sh:48` — unpinned `nixpkgs#wlrctl` fallback (non-reproducible).
- `shellbridge.nix:174` — sessions.json schema comment omits contextTokens/contextCeiling/needsSudo.
- `vmTest.nix:186` — comment says "54 commands", assert is 60.
- `CONTRACTS.md:192` — §3 example uses the cut `rice gen` path.
- `pointer.nix` + `tools/` are untracked — flake eval uses the git index; stage them before flipping `aoide.pointer.enable`.
- `flake.lock` diff not verifiable by the fleet (no git in reviewers); the lock itself evals clean per `nix flake check` run separately.

## Verified clean (no finding)

- Audio `voices`→`channels` rename is complete — zero stale `VoiceRow`/`voiceCount`/`CueVoice` references repo-wide; remaining "voices" are unrelated domains (typography, gauges, conductor tally).
- Audio roster single-sourced in bar.qml, passed to AudioColonnade as props — no duplicated enumeration.
- AGENTS.md ↔ AOIDE-DEV.md ↔ pointer.nix ↔ pointer.sh ↔ hazards.md §6 are mutually coherent.
- pointer.nix is dendrite-shape v0 legal; no module reads another module.
- `aoide schema --json` single-sourced from the Registry (60 paths, vmTest asserts 60).
- Window listener has 5s read-timeout + 2s reconnect backoff; conduct poll is 1s/blocking, no spin.
- TerminalsGadget/AoideNotifications avoid the Conductor's delegate churn.
