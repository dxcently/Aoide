# sonata's `power` slot — the compact battery/power-vitals stele. `kind`
# absent → null: no `WidgetSlot { slot: "power" }` anchor is wired in the
# shipped dock yet, so it registers nothing today either way. No sibling
# slots embedded, no uppercase helper instantiated.
_: {
  file = "power.qml";
}
