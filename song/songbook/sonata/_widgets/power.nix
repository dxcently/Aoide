# sonata's `power` slot — the compact battery/power-vitals stele. `kind`
# absent → null: facet-anchored, never a declared registry entry. Hosted by
# sonata's own `widgets/dock.qml` (the shipped dock), one
# `WidgetSlot { slot: "power" }` in its gadget column. No sibling slots
# embedded, no uppercase helper instantiated.
_: {
  file = "power.qml";
}
