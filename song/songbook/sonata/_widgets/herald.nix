# sonata's `herald` slot — the notification popup, hosted by shell.qml's
# `SurfaceSlot { slot: "herald" }` (slots.md's wired table). `kind` absent →
# null: facet-anchored, never a declared registry entry. `AudioColonnade` is
# named only in a header comment (borrowing its signature hue); it is not
# instantiated.
_: {
  file = "herald.qml";
}
