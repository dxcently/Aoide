[package]
name = "aoide-test-support"
version = "0.0.0"
edition = "2021"
license = "MIT"
description = "Aoide shared test scaffolding: scratch dirs, EnvSaver, fixture note payloads, the process-wide env lock. Dev-dependency only — never a production edge."

[lib]
name = "aoide_test_support"
path = "src/lib.rs"

[dependencies]
aoide-protocol = { workspace = true }
