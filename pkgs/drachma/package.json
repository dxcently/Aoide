{
  "name": "aoide-drachma",
  "version": "0.0.0",
  "description": "Aoide drachma: the note engine — Style Dictionary resolver + v0 schema lint + live-side emitters (stage/notes.json, hyprctl, terminal OSC).",
  "license": "MIT",
  "type": "commonjs",
  "bin": {
    "drachma": "src/cli.js"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "node src/cli.js lint test/fixtures/valid.json"
  },
  "dependencies": {
    "style-dictionary": "4.3.0"
  },
  "engines": {
    "node": ">=18"
  }
}
